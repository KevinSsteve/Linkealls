/**
 * Reversible launch-scope policy. Keep these values aligned with the API
 * server policy so dormant legacy surfaces are never mounted while disabled.
 */
export const ADVERTISING_NEW_ACTIONS_ENABLED = false;
export const NONESSENTIAL_SUMMARIES_ENABLED = false;